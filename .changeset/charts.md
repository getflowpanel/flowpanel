---
"@flowpanel/charts": minor
---

Empty charts use the configured labels, a skeleton holds the space until the chart mounts, pies show their legend by default, and tooltips and ticks format currency and dates with the admin's `formatting`.
