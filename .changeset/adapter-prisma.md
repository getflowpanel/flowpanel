---
"@flowpanel/adapter-prisma": minor
---

Prisma gains real `flowpanel migrate` support, creates that respect a filter-shaped tenant scope instead of failing, and introspection that reports database-generated ids the way Drizzle does.
