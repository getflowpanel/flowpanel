---
"@flowpanel/cli": minor
---

`init`, `doctor`, `migrate` and `new` scaffold the code the documentation teaches, and a dry run no longer touches the database. A kit and CLI version mismatch is now a hard stop.
