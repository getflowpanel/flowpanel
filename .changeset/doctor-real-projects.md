---
"@flowpanel/cli": patch
---

Check active core installations by following the installed dependency graph instead
of counting stale pnpm store directories. Resolve intercepted URL segments and
allow static admin branches alongside dynamic locale and catch-all fallbacks,
while protecting existing routes inside the selected admin branch.
