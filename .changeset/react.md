---
"@flowpanel/react": minor
---

Cards for the new widgets, a dashboard freshness stamp, headers that wrap on narrow screens, a pager that shows its range and a drawer link to the record page. The reference picker no longer depends on `cmdk`, taking the UI bundle from 72.3 kB to 69.6 kB brotli. `formatNumber` takes the resolved `formatting`; a locale string still works.
