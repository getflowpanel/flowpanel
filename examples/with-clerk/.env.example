# Clerk — create an app at https://dashboard.clerk.com and copy the keys.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxx
CLERK_SECRET_KEY=sk_test_xxx

# Postgres — matches docker-compose.yml (port 54330 to avoid collision with
# other examples).
DATABASE_URL=postgres://fp:fp@localhost:54330/with_clerk
