# @flowpanel/cli

Internal CLI for FlowPanel — `init`, `migrate`, `doctor`.

**Not published separately.** This binary is bundled with `flowpanel`; run via:

    pnpm dlx flowpanel <command>
    # or, if installed locally:
    pnpm flowpanel <command>
