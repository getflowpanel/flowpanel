/**
 * Ejected list view for the "{{name}}" resource.
 *
 * This file was generated by `flowpanel eject resource {{name}}`. The
 * runtime no longer renders /admin/{{name}} — your code does. Customize
 * freely; FlowPanel will not touch this file again.
 *
 * Data still goes through FlowPanel's protected request controller, so auth,
 * field policies, tenant scope and adapter projection stay identical to the
 * generated UI. Replace only the rendering below.
 */
import { listRows } from "./actions";

export default async function Page() {
  const result = await listRows();
  return (
    <div className="space-y-4 p-6">
      <h1 className="text-xl font-semibold">{{name}}</h1>
      <pre className="overflow-auto rounded-fp-lg border border-fp-border-1 bg-fp-bg-1 p-4 text-xs">
        {JSON.stringify(result.rows, null, 2)}
      </pre>
    </div>
  );
}
