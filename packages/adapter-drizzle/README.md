# @flowpanel/adapter-drizzle

Internal Drizzle ORM adapter for FlowPanel.

**Not published separately.** Users install [`flowpanel`](../flowpanel) and import from `flowpanel/drizzle`.

Implements the `Adapter` contract from `@flowpanel/core`: introspect, inferSchema, list, get, create, update, delete.
