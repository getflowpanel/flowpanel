# Reverse-coverage allowlist for scripts/check-docs.ts.
#
# Every export of every non-private package barrel must appear (by literal
# identifier, word-boundary match) somewhere in apps/site/content/docs, or be
# listed here. This is a ratchet: shrinking it is always safe; growing it
# needs review — new entries should fall into one of the sections below, or
# get their own docs mention instead.
#
# `# TODO(docs):` entries are exports that look like they should have their
# own docs mention but don't yet. They're allowlisted so the gate stays
# green, but they're tracked debt — see task-3.3-report.md for the list.

# --- @flowpanel/core: type-level plumbing -----------------------------
# Generic-constraint helpers for the FlowpanelResources module-augmentation
# pattern. Used implicitly wherever `resource()`/`reference()` are typed;
# never imported by name in application code, so docs never reference the
# literal identifier.
ResourceName
ReferenceSpec
StructuredFilterValue

# --- @flowpanel/core: labels internals ---------------------------------
# core-concepts/configuration.mdx and reference/define-config.mdx document
# the `labels` config option and its shape; these are the implementation
# behind it (default table, merge function, resolved-type alias), not
# something a resource author imports directly.
DEFAULT_LABELS
formatLabel
mergeLabels
ResolvedLabels

# --- @flowpanel/core: widget builder return types -----------------------
# customization/charts.mdx and core-concepts/dashboards.mdx document the
# builder functions (areaChart, barChart, lineChart, pieChart, metric,
# statGroup) in full; these are just those builders' return types.
AreaChartWidget
BarChartWidget
LineChartWidget
MetricWidget
PieChartWidget
StatGroupWidget

# TODO(docs): @flowpanel/core — runtime helpers for custom adapter/route
# authors. Audit and role-check *concepts* are documented (permissions.mdx,
# configuration.mdx), but these specific exported functions aren't
# mentioned anywhere — someone writing a custom adapter or route handler
# has no docs pointer to them.
emitAudit
checkRequireRole

# --- @flowpanel/next: route ctx/param types -----------------------------
# reference/next-runtime.mdx documents drawerRoute/drawerActionRoute and
# buildRequestContext; these are their param/context interfaces.
DrawerRouteCtx
BuildRequestCtxArgs

# --- @flowpanel/react: Props types of documented components ------------
# The component itself has a docs mention (reference/react-components.mdx
# or elsewhere); its *Props type is the same surface, not separate surface.
LiveIndicatorProps
LocalTimeProps
SparklineProps
TimeAgoProps
ArrayCellProps
BulkBarProps
ColumnPinMenuProps
ColumnVisibilityMenuProps
DateRangePickerProps
DensityToggleProps
FilterBarProps
BooleanFilterProps
DateRangeFilterProps
MultiSelectFilterProps
NumericRangeFilterProps
SelectFilterProps
TagFilterProps
TextFilterProps
InlineEditCellProps
JsonCellProps
JsonEditorProps
MobileCardListProps
ReferenceCellProps
ReferencePickerProps
ErrorStateProps
HealthBannerProps
DashboardSkeletonProps
ResourceDetailSkeletonProps
ResourceListSkeletonProps
AsyncSelectProps
FormSectionProps
MetricGridProps
DividerProps
SectionLabelProps
SectionProps
AccountMenuProps
BrandProps
BreadcrumbsProps
DetailShellProps
FlowpanelGlobalsProps
ShortcutsCheatsheetProps
ThemeScriptProps
CustomWidgetProps
StatGroupCardProps
TableWidgetProps
RealtimeProviderProps

# --- @flowpanel/react: supporting types of documented components/hooks --
# Tone/sort/column/item/state/option shapes and hook option-or-result
# aliases. The component or hook they belong to has its own docs mention;
# these are the data shapes that flow through it.
BadgeTone
StatusBadgeTone
StatusTone
BulkBarAction
PinSide
ColumnVisibilityMenuColumn
DataTableColumn
DataTableSort
MultiSelectFilterOption
SelectFilterOption
ReferenceItem
ToastApi
AsyncSelectOption
AccountMenuItem
AccountMenuUser
NavEntry
CommandGroupUI
ShortcutSpec
AdminCommand
AdminDrawer
AdminDrawerState
TableSort
UseAdminTable
UseDashboardParamResult
UseLiveChannelOptions
RealtimeChannels
UseRealtimeRefreshOptions
UseTheme
UseThemeOptions
UrlState
ThemeChoice
ThemeMode
DownloadPayload
RealtimeStats
RealtimeStatus

# --- @flowpanel/react: shadcn/Radix UI primitives -----------------------
# reference/react-components.mdx ("UI primitives" section) documents
# Dialog, Popover, Select, DropdownMenu, Sheet, Tooltip, Tabs* collectively
# as shadcn-derived re-exports whose API is upstream shadcn/Radix docs, not
# ours — it deliberately does not enumerate every sub-part export.
DialogClose
DialogContent
DialogDescription
DialogFooter
DialogHeader
DialogOverlay
DialogPortal
DialogTitle
DialogTrigger
DropdownMenuCheckboxItem
DropdownMenuContent
DropdownMenuGroup
DropdownMenuItem
DropdownMenuLabel
DropdownMenuSeparator
DropdownMenuShortcut
DropdownMenuSub
DropdownMenuSubContent
DropdownMenuSubTrigger
DropdownMenuTrigger
PopoverAnchor
PopoverContent
PopoverTrigger
SelectContent
SelectGroup
SelectItem
SelectLabel
SelectSeparator
SelectTrigger
SelectValue
SheetClose
SheetContent
SheetContentProps
SheetDescription
SheetFooter
SheetHeader
SheetOverlay
SheetPortal
SheetTitle
SheetTrigger
TabsContent
TabsList
TabsTrigger
TooltipContent
TooltipProvider
TooltipTrigger

# TODO(docs): @flowpanel/react — RealtimeRefresh is a declarative component
# wrapper around useRealtimeRefresh (which IS documented with a usage
# example in reference/react-components.mdx). The component form itself
# has no docs mention.
RealtimeRefresh
